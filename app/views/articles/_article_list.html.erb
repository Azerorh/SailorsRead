<div class="article">
  <h4><%= article.title %><span class="text-wrap" style="float: right;margin-top: 10px;">Author: <%= author(article) %></span></h4>

  <p><%= article.text %></p>
  <h6><%= link_to 'Show', article_path(article), class: 'btn btn-primary' %>
  <% if is_admin? && user_signed_in? %> 
    <%= link_to 'Edit', edit_article_path(article), class: 'btn btn-primary' %> 
    <%= link_to 'Delete', article_path(article), method: :delete, class: 'btn btn-primary' %> 
  <% end %>
  <span class="text-wrap" style="float: right;margin-top: 10px;">Comments: <%= comments_counter(article) %></span></h6>
</div><hr>